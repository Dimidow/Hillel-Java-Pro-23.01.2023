public class HomeWorkApp {

    public static void main(String[] args) {
        Tasks task = new Tasks();
        task.printThreeWords();
        task.checkSumSign();
        task.printColor();
        task.compareNumbers();
        task.isSumLiesInInterval(13, 8);
        task.checkPositiveOrNegativeNumber(-1);
        task.isPositiveOrNegativeNumber(0);
        task.printSpecifiedNumberOfTimes(13, 5);
        task.isYearLeap(400);
    }
}